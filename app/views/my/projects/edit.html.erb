<%= form_for [:my, @project] do |f| %>
	
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.submit "Update Project" %>

<% end %>

<%= link_to 'Delete Project', [:my, @project], :method => :delete, :class => 'btn btn-small btn-danger', data: {confirm: "Are you sure?"} %>
